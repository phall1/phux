schema_version = 1
id = "codex"
display_name = "Codex"
kind = "terminal-agent"
description = "OpenAI Codex CLI running inside a phux-managed terminal."
package_version = "0.1.0"
package_status = "public"
first_party = true
capabilities = ["terminal-control", "code-editing", "human-question"]

[detect]
mode = "opt-in"
command = "codex"
env = "PHUX_AGENT_TOOLS_DETECT"

[link]
mode = "state-file"
default_session = "agent-bench-codex"

[session_identity]
mode = "native-or-phux"
native_env = "PHUX_CODEX_SESSION_ID"
restore = "external-cli"

# phux-r82.11: launch through the agent wrapper so the pane self-declares
# a `phux.agent/v1` record (ADR-0040) — name + kind land automatically, so
# the sidebar/fleet show a first-class identity instead of the OSC-title
# substring heuristic. Clearing on exit is handled by the wrapper's trap.
[agent_identity]
name = "codex"
kind = "codex"

[launch]
command = ["sh", "scripts/phux-agent-wrap.sh", "--name", "codex", "--kind", "codex", "--", "codex"]
working_directory = "workspace"
