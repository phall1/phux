# Compatibility alias. `herdr` was renamed to `starter`; this file exists
# so configs that still extend distros/herdr/herdr.toml keep loading.
# Do not add settings here — they belong in starter.toml or the shipped
# defaults. See distros/starter/README.md.
extends = ["../starter/starter.toml"]
